---
- name: Manage server
  hosts: servers
  remote_user: root

  tasks:
    - name: wait for connection
      wait_for_connection:
      
    - name: install ranger
      apt:
        name: ranger
        require:
          - wait_for_connection